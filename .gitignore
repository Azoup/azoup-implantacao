node_modules
dist
.cursor/
*.local
.DS_Store

# Segredos (Supabase, etc.) — nunca enviar para o GitHub
.env
.env.local
.env.*.local

# Cache local do Supabase CLI (project-ref, pooler, versões) — não versionar
supabase/.temp/
only_read_old_vyntask/
